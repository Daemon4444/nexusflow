# NexusFlow — Claude Code 入口

在分析、修改或部署本项目之前：

1. 完整阅读根目录 [`WIKI.md`](WIKI.md)，建立项目全景；
2. 阅读 [`AGENTS.md`](AGENTS.md)，遵守计费、安全、验证和部署规则；
3. 再读取与当前任务直接相关的代码和专项文档；
4. 历史 Claude 记忆只是决策档案，当前代码、migrations 和生产探针优先。

不要把 `.env`、密钥、客户数据或请求正文写入仓库。项目事实发生变化时，同步更新 `WIKI.md`。
