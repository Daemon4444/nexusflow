# Shared public API flood guards. This file is loaded in nginx's http context.
# The ALB client IP is restored by each site's trusted real_ip configuration.
limit_req_zone $binary_remote_addr zone=nf_v1_edge_v2:10m rate=1000r/s;
limit_conn_zone $binary_remote_addr zone=nf_v1_edge_conn_v2:10m;
